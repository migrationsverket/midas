name: 'Publish'

permissions:
  contents: write
  pages: write
  id-token: write

on:
  push:
    tags:
      - '*@v*.*.*'

jobs:
  publish-to-npm:
    uses: migrationsverket/midas/.github/workflows/publish-to-npm.yml
  publish-docs:
    if: ${{ contains(github.ref_name, 'components@') }}
    uses: migrationsverket/midas/.github/workflows/publish-docs.yml
