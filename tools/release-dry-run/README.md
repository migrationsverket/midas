# release-dry-run

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build release-dry-run` to build the library.

## Running unit tests

Run `nx test release-dry-run` to execute the unit tests via [Jest](https://jestjs.io).
