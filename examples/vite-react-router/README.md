# react + typescript + react-router + @midas-ds/layout + @midas-ds/components

This example shows a minimal setup to use @midas-ds packages together with `vite` and `react-router`

Run `npm install` to get started, `npm run dev` starts up the development server. See package.json for more scripts.
