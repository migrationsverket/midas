{
  "name": "next",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": ["apps"],
  "targets": {
    "install-local": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/next && npm install --@midas-ds:registry=http://localhost:4873/"
      }
    },
    "e2e": {
      "dependsOn": [
        { "projects": ["@midas-ds/source"], "target": "release:local:dev" },
        "install-local"
      ]
    }
  }
}
