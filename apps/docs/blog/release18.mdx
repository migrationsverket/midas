---
title: 'Release 18'
description: .
slug: release-18
---

import LiveCodeBlock from '@site/src/components/CodeBlock/CodeBlock'
import { Flex, FlexItem } from '@midas-ds/flex'
import { Link } from '@midas-ds/link'
import { TextField } from '@midas-ds/textfield'
import { Button, ButtonGroup } from '@midas-ds/button'
import { GlobalToastRegion, toastQueue } from '@midas-ds/toast'

Nedan är en sammanfattning av nyheterna i release 18 .

{/* truncate */}

## API på komponenter är tillbaka

Nu finns efter en tids uppehåll API och props för varera komponent beskrivna på komponentsidorna.

## Instruktioner för klientnavigering (Client Side Routing)

Tydligare instruktioner och fler möjligheter för att integrera komponenterna med din router finns nu på [denna sida](/dev/client-side-routing)

## Nya varianter på Logo

Komponenten [Logo](/components/logo) har fått nya storlekar och varianter.

## Sök

Du kan nu söka på denna site. Du finner söken högst upp i högra hörnet eller med kortkommandot `ctrl` + `K` alternativt på Mac `⌘` + `K`.

## Mindre justeringar

- Justerat och förbättrat hur Modal växer beroende på innehåll.

## Uppdaterade komponenter

-
