---
id: icons
title: Ikoner
sidebar_position: 3
---

import {
  Plus,
  Minus,
  Check,
  LogIn,
  LogOut,
  Trash2,
  Sun,
  Moon,
  Laptop,
  Info,
  Pencil,
  Flag,
  TriangleAlert,
  Search,
  X,
  CalendarDays,
  ArrowRight,
  ArrowLeft,
  ChevronDown,
  ChevronUp,
  SquareArrowOutUpRight,
  ArrowDownToLine,
  Menu,
  EllipsisVertical,
  Ellipsis,
  Bell,
  House,
  Languages,
} from 'lucide-react'
import { CopyButton } from '@site/src/components/CopyButton'
import { Tooltip, TooltipTrigger } from '@midas-ds/components'

Ikoner används som ett komplement till text för att underlätta för användaren, inte för enbart dekoration. Rätt använda kan ikoner göra funktioner och innehåll tydligare och hjälpa användare att snabbt hitta rätt i grännssnittet.

Midas använder [Lucide Icons](https://lucide.dev/guide/packages/lucide-react) som ikonbibliotek. Vi använder ikonerna direkt från Lucide, utan några modifieringar.

:::info[Egna ikoner]
Om du har behov av en ikon som inte finns i Lucide icons, kontakta Midas-teamet för analys av behovet och möjliga lösningar.
:::

## Riktlinjer

- För att undvika tvetydighet, använd en och samma symbol per funktion. Vi har en uppsättning [standardikoner](#standardikoner) på Migrationsverket som ska användas på samma sätt i alla system och tjänster.
- Färgen på ikoner sätts via våra [ikonfärger](/basics/color/#ikoner). Inga egna färger utanför tokens ska användas.
- Standardstorleken på ikonerna är `20px` (`1.25rem`). Om textstorleken är liten eller ikonen används i en tabell kan `16px` (`1rem`) användas istället.
- Ikonen ligger som standard till vänster om den tillhörande texten.
- Dölj ikonen för skärmläsare om den bara kompletterar en befintlig text. Om ikonen är interaktiv eller bär på egen information måste den i stället ha en egen textbeskrivning.

### I externa tjänster

I externa tjänster bör ikoner användas restriktivt. Det beror på att igenkänningseffekten inte är lika stor för sällananvändare i externa tjänster som den är i interna system där användaren ser dem dagligen. När ikoner används bör de alltid kombineras med text för ökad tydlighet.

### I interna system

För interna system där användarna är bekanta med gränssnittet och återkommer till det ofta kan ikoner användas utan text för att effektivisera och spara plats. Men rekommendationerna är att kombinera ikonen med text och att vara försiktig med ikoner som inte är allmänt vedertagna. Testa med användare om du är osäker.

### Färger på ikoner

När ikoner används tillsammans med text bör de ha samma färg som den kompletterande texten. När en ikon används för att kommunicera status används tokens success, info, important och warning, se [ikonfärger](/basics/color/#ikoner). Tänk på att inte enbart använda färg för att komunicera status och att färgen måste ha tillräcklig kontrast mot bakgrunden (4,5:1).

## Standardikoner

För att säkerställga enhetlighet och tydlighet har vi ett antal standardikoner som ska användas på samma sätt i alla tjänster och system.

### Navigation

| Ikon                          | Namn                                   | Användning      |
| :---------------------------- | :------------------------------------- | :-------------- |
| <Menu size={20}/>             | <CopyButton text='Menu' />             | Huvudmeny, meny |
| <EllipsisVertical size={20}/> | <CopyButton text='EllipsisVertical' /> | Meny            |
| <Ellipsis size={20}/>         | <CopyButton text='Ellipsis' />         | Meny            |

### Pilar/riktning

| Ikon                     | Namn                              | Användning        |
| :----------------------- | :-------------------------------- | :---------------- |
| <ArrowRight size={20}/>  | <CopyButton text='ArrowRight' />  | Höger, nästa      |
| <ArrowLeft size={20}/>   | <CopyButton text='ArrowLeft' />   | Vänster, tillbaka |
| <ChevronDown size={20}/> | <CopyButton text='ChevronDown' /> | Fäll ut, nedåt    |
| <ChevronUp size={20}/>   | <CopyButton text='ChevronUp' />   | Fäll ihop, uppåt  |

### Status

| Ikon                       | Namn                                | Användning         |
| :------------------------- | :---------------------------------- | :----------------- |
| <Check size={20}/>         | <CopyButton text='Check' />         | Ok, slutfört       |
| <Info size={20}/>          | <CopyButton text='Info' />          | Information        |
| <Flag size={20}/>          | <CopyButton text='Flag' />          | Viktig information |
| <TriangleAlert size={20}/> | <CopyButton text='TriangleAlert' /> | Varning, fel       |

### Action

| Ikon                | Namn                         | Användning      |
| :------------------ | :--------------------------- | :-------------- |
| <Plus size={20}/>   | <CopyButton text='Plus' />   | Lägg till, öka  |
| <Minus size={20}/>  | <CopyButton text='Minus' />  | Ta bort, minska |
| <Trash2 size={20}/> | <CopyButton text='Trash2' /> | Ta bort, släng  |
| <Pencil size={20}/> | <CopyButton text='Pencil' /> | Redigera        |
| <X size={20}/>      | <CopyButton text='X' />      | Stäng, avbryt   |
| <Search size={20}/> | <CopyButton text='Search' /> | Sök             |

### Länkar/Media

| Ikon                               | Namn                                        | Användning  |
| :--------------------------------- | :------------------------------------------ | :---------- |
| <ArrowDownToLine size={20}/>       | <CopyButton text='ArrowDownToLine' />       | Ladda ner   |
| <SquareArrowOutUpRight size={20}/> | <CopyButton text='SquareArrowOutUpRight' /> | Extern länk |

### Övrigt

| Ikon                      | Namn                               | Användning               |
| :------------------------ | :--------------------------------- | :----------------------- |
| <LogIn size={20}/>        | <CopyButton text='LogIn' />        | Logga in                 |
| <LogOut size={20}/>       | <CopyButton text='LogOut' />       | Logga ut                 |
| <CalendarDays size={20}/> | <CopyButton text='CalendarDays' /> | Kalender, datum, bokning |
| <Bell size={20}/>         | <CopyButton text='Bell' />         | Notifiering              |
| <House size={20}/>        | <CopyButton text='House' />        | Hem                      |
| <Languages size={20}/>    | <CopyButton text='Languages' />    | Språkväxlare             |
| <Moon size={20}/>         | <CopyButton text='Moon' />         | Mörkt läge               |
| <Sun size={20}/>          | <CopyButton text='Sun' />          | Ljust läge               |
