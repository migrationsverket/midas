---
title: Accordion
sidebar_position: 1
description: Dragspelskomponenten låter användare visa och dölja delar av relaterat innehåll på en sida.
---

import { PropTable } from '@site/src/components/propsTable'
import { Accordion, AccordionItem } from '@midas-ds/components'
import { ComponentHeader } from '@site/src/components/getComponentMetaData'
import LiveCodeBlock from '@site/src/components/CodeBlock/CodeBlock'

export const Example = props => {
  return (
    <LiveCodeBlock
      scope={{ Accordion, AccordionItem }}
      {...props}
    >
      {`<Accordion>
        {fruits.shuffled().map(fruit => (
          <AccordionItem key={fruit.value} id={fruit.value} title={fruit.name}>
              {fruit.description}
          </AccordionItem>
        ))}
      </Accordion>`}
    </LiveCodeBlock>
  )
}

<ComponentHeader
  name={'Accordion'}
  friendlyName={'Dragspel'}
  overrideHeadlessLink='https://react-spectrum.adobe.com/react-aria/Disclosure.html'
/>

Utfällbar komponent som används för att minska informationsmängden som direkt presenteras för användaren.

<Example hideCode />

## Installation

```bash npm2yarn
npm install @midas-ds/components
```

```tsx
import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@midas-ds/components'
```

<Example hideExample />

## Egenskaper

Accordion har flertalet varianter och egenskaper.

### Contained

En tydligare variant av layouten när det finns en tydlig gräns mellan varje sektion.

<LiveCodeBlock scope={{ Accordion, AccordionItem }}>
  {`<Accordion variant="contained">
    {fruits.shuffled().map(fruit => (
      <AccordionItem key={fruit.value} id={fruit.value} title={fruit.name}>
          {fruit.description}
      </AccordionItem>
    ))}
</Accordion>`}
</LiveCodeBlock>

### Flera öppna samtidigt

En accordion har som standard bara en öppet samtidigt och stänger automatiskt andra sektioner när en ny öppnas. Detta kan ändras genom att sätta `type` till `multiple`.

<LiveCodeBlock scope={{ Accordion, AccordionItem }}>
  {`<Accordion type="multiple">
    {fruits.shuffled().map(fruit => (
      <AccordionItem key={fruit.value} id={fruit.value} title={fruit.name}>
          {fruit.description}
      </AccordionItem>
    ))}
</Accordion>`}
</LiveCodeBlock>

### Öppen som standard

En accordion kan ha en eller flera sektioner öppna som standard. Detta görs genom att sätta `defaultExpandedKeys` på `Accordion`. Detta är en array med nycklar för de sektioner som ska vara öppna som standard. Nycklarna bestäms med `id` på respektive `AccordionItem`.

<LiveCodeBlock scope={{ Accordion, AccordionItem }}>
  {`<Accordion type="multiple" defaultExpandedKeys={[2]}>
    {fruits.shuffled().map((fruit, i) => (
      <AccordionItem key={fruit.value} id={i} title={fruit.name}>
          {fruit.description}
      </AccordionItem>
    ))}
  </Accordion>`}
</LiveCodeBlock>

## Riktlinjer

- Dragspelet är komprimerat som standard och expanderas genom att klicka på rubrikytan.

## API

### Accordion

<PropTable name={'Accordion'} />

### AccordionItem

<PropTable
  name={'AccordionItem'}
  defaultOpen={false}
/>
