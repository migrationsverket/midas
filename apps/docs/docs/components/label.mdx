---
title: Label
---

import { Label } from '@midas-ds/components'
import { PropTable } from '@site/src/components/propsTable'
import { ComponentHeader } from '@site/src/components/getComponentMetaData'
import LiveCodeBlock from '@site/src/components/CodeBlock/CodeBlock'

export const Example = props => {
  return (
    <LiveCodeBlock
      scope={{ Label }}
      {...props}
    >
      {`<Label>Jag är ett en vanlig etikett</Label>`}
    </LiveCodeBlock>
  )
}

<ComponentHeader
  name={'typography-label'}
  friendlyName={'Etikett'}
  overrideHeadlessLink='https://github.com/adobe/react-spectrum/tree/main/packages/@react-aria/label'
/>

Etikett med fördefinerad stil

<Example hideCode />

## Installation

```bash npm2yarn
npm install @midas-ds/components
```

```tsx
import { Label } from '@midas-ds/components'
```

<Example hideExample />

## Varianter

### Stil

Använd egenskapen `variant` för att justera etikettens utseende

<LiveCodeBlock
  scope={{ Label }}
  {...props}
>
  {`<Label variant="label-02">Jag är en etikett av varianten 'label-02'</Label>`}
</LiveCodeBlock>

### Val av element

Egenskapen `elementType` kan användas för att ändra etikettens DOM-element.

<LiveCodeBlock
  scope={{ Label }}
  {...props}
>
  {`<Label elementType="div">Jag är en div</Label>`}
</LiveCodeBlock>

## API

### Label

<PropTable name='Label' />
