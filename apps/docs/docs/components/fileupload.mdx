---
title: FileUpload
description: Filuppladdning används när användaren behöver ladda upp en eller flera filer från det egen filsystemet.
---

import { PropTable } from '@site/src/components/propsTable'
import { FileUpload } from '@midas-ds/components'
import CodeBlock from '@theme/CodeBlock'
import { ComponentHeader } from '@site/src/components/getComponentMetaData'
import LiveCodeBlock from '@site/src/components/CodeBlock/CodeBlock'

export const Example = props => {
  return (
    <LiveCodeBlock
      scope={{ FileUpload }}
      {...props}
    >
      {`<FileUpload allowsMultiple label={'Välj filer att ladda upp'} description={'Du kan välja flera filer!'}/>`}
    </LiveCodeBlock>
  )
}

<ComponentHeader
  name={'FileUpload'}
  friendlyName={'Filuppladdning'}
  overrideHeadlessLink='https://react-spectrum.adobe.com/react-aria/FileTrigger.html'
/>

Komponent som används när användaren behöver ladda upp filer från det egen filsystemet.

<Example hideCode />

## Installation

```bash npm2yarn
npm install @midas-ds/components
```

```tsx
import { FileUpload } from '@midas-ds/components'
```

<Example hideExample />
