---
title: FileUpload
description: Filuppladdning används när användaren behöver ladda upp en eller flera filer från det egen filsystemet.
tags: [form]
---

import { PropTable } from '@site/src/components/propsTable';
import { FileUpload } from '@midas-ds/file-upload';
import CodeBlock from '@theme/CodeBlock';
import { ComponentHeader, ComponentFooter } from '@site/src/components/getComponentMetaData';
import Info from '@site/static/data/file-upload.json'
import LiveCodeBlock from '@site/src/components/CodeBlock/CodeBlock'


<ComponentHeader name={'FileUpload'}  info={Info} friendlyName={'Filuppladdning'} overrideHeadlessLink="https://react-spectrum.adobe.com/react-aria/FileTrigger.html" />

## Introduktion
Filuppladdning används när användaren behöver ladda upp en eller flera filer från det egen filsystemet.

:::info
Filuppladdning är i experimentell status för UX-testning och fungerar inte rent tekniskt. Det är rimligt att anta att både implementation och uppbyggnad av template kommer att förändras.
Komponenten bygger på [React Aria FileTrigger](https://react-spectrum.adobe.com/react-aria/FileTrigger.html)

**TODO:s**

- hantera `onSelect()` för att ge användare möjlighet att själv sätta uppladdningsmetod
- hantera `invalid` state? Beror på use case om detta är tillämpbart
- implementation i `<form>`
- tillgänglighet gällande button / label

:::



## Installation och användning

```bash npm2yarn
npm install @midas-ds/fileupload
```

```tsx
import { FileUpload } from '@midas-ds/fileupload';
```
<LiveCodeBlock scope={{FileUpload}}>
{`<FileUpload allowsMultiple label={'Etikett'} description={'Beskrivning'}/>`}
</LiveCodeBlock>


## Dependencies

<ComponentFooter info={Info}/>