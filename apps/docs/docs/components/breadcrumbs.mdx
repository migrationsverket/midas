---
title: Breadcrumbs
description: Brödsmulor används för att visa användaren vart hen är i ett djupt navigationsträd.
---

import { PropTable } from '@site/src/components/propsTable'
import { Breadcrumbs } from '@midas-ds/components'
import { ComponentHeader } from '@site/src/components/getComponentMetaData'
import LiveCodeBlock from '@site/src/components/CodeBlock/CodeBlock'

export const Example = props => {
  return (
    <LiveCodeBlock
      scope={{ Breadcrumbs }}
      {...props}
    >
      {`<Breadcrumbs items={fruits.shuffled().map(fruit => {return {href: fruit.value, title: fruit.name}})} />`}
    </LiveCodeBlock>
  )
}

<ComponentHeader
  name={'Breadcrumbs'}
  friendlyName={'Brödsmulor'}
/>

Komponent som används för att visa användaren var den är i ett djupt navigationsträd.

<Example hideCode />

## Installation

```bash npm2yarn
npm install @midas-ds/components
```

```tsx
import { Breadcrumbs } from '@midas-ds/components'
```

<Example hideExample />

## API

<PropTable name='Breadcrumbs' />
