---
title: Skeleton
description: Laddningsindikator för komponenter
---

import { PropTable } from '@site/src/components/PropsTable'
import { ComponentHeader } from '@site/src/components/getComponentMetaData'
import { Skeleton } from '@midas-ds/components'
import { variables } from '@midas-ds/theme'

<ComponentHeader
  name='Skeleton'
  friendlyName='Laddningsindikator'
  overrideHeadlessLink=''
/>

Komponent som används som en platshållare för den riktiga komponenten innan sidan laddat klart

```tsx
import { Skeleton } from '@midas-ds/components'
```

```tsx
<Skeleton height='48px' />
```

<div className='card'>
  <Skeleton height='48px' />
</div>

## Varianter

### Rectangle

Detta är standardvarianten. Används som platshållare för text och de flesta komponenter vi har. Ange storleken i px, rem eller % med `width` och `height`. Om inget anges kommer den att fylla sin parent container.

```tsx
<Skeleton height='48px' />
```

<div className='card'>
  <Skeleton height='48px' />
</div>

### Circle

Används för cirkulära komponenter, tex avatar. Circle behöver bara `width` och sätter automatiskt höjden så att den blir rund.

```tsx
<Skeleton
  // highlight-start
  variant='circle'
  // highlight-end
  width='48px'
/>
```

<div className='card'>
  <Skeleton
    variant='circle'
    width='48px'
  />
</div>

### Form

Används som platshållare för formulärfält som inte laddat klart än. Form-varianten visar en skeleton för etikett, beskrivning och inmatningsfält som liknar riktiga formulärkomponenter som TextField.

```tsx
<Skeleton
  // highlight-start
  variant='form'
  // highlight-end
  width='400px'
/>
```

<div className='card'>
  <Skeleton
    variant='form'
    width='400px'
  />
</div>

#### Storlek

Form-varianten stödjer två storlekar för inmatningsfältet: `large` (48px, standard) och `medium` (40px).

```tsx
<Skeleton
  variant='form'
  // highlight-start
  size='medium'
  // highlight-end
  width='400px'
/>
```

<div className='card'>
  <Skeleton
    variant='form'
    size='medium'
    width='400px'
  />
</div>

#### Flera formulärfält

För att visa en laddningsindikator för flera formulärfält, använd flera Skeleton-komponenter med form-varianten.

```tsx
<div style={{ display: 'flex', flexDirection: 'column', gap: '2rem' }}>
  <Skeleton variant='form' />
  <Skeleton variant='form' />
  <Skeleton variant='form' />
</div>
```

<div className='card'>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '2rem' }}>
    <Skeleton variant='form' />
    <Skeleton variant='form' />
    <Skeleton variant='form' />
  </div>
</div>

## Animation

Skeleton är animerad som standard. Det går att stänga av med `isAnimated={false}`.

```tsx
<Skeleton
  height='48px'
  // highlight-start
  isAnimated={false}
  // highlight-end
/>
```

<div className='card'>
  <Skeleton
    height='48px'
    isAnimated={false}
  />
</div>

## Att använda en annan bakgrundsfärg

Om Skeletonkomponenten ligger på bakgrundsfärgen `layer01` eller `light-dark(#f2f2f2, #262626)`, behöver den använda propen `isOnLayer01` för att synas.

```tsx
<Skeleton
  height='48px'
  // highlight-start
  isOnLayer01
  // highlight-end
/>
```

<div
  className='card'
  style={{ backgroundColor: variables.layer01Base, padding: '1rem' }}
>
  <Skeleton
    height='48px'
    isOnLayer01
  />
</div>

## Riktlinjer

För val av laddningsindikator se [Mönster för laddningsindikatorer](/design-patterns/page-loading)

## API

<PropTable name='Skeleton' />
