---
title: Spinner
description: Upplys användaren om att systemet/tjänsten laddar
---

import { PropTable } from '@site/src/components/propsTable'
import { ComponentHeader } from '@site/src/components/getComponentMetaData'
import { Spinner, Button } from '@midas-ds/components'

<ComponentHeader
  name='Spinner'
  friendlyName='Laddningsindikator'
  overrideHeadlessLink=''
/>

Komponent som används för att upplysa användaren om att systemet/tjänsten laddar eller jobbar på något i bakgrunden.

```tsx
<Spinner />
```

<div className='card'>
  <Spinner />
</div>

## Installation

```bash npm2yarn
npm install @midas-ds/components
```

```tsx
import { Spinner } from '@midas-ds/components'
```

## Varianter

### Fristående spinner

```tsx
<Spinner />
```

<div className='card'>
  <Spinner />
</div>

### Kombinerad med andra komponenter

Använd varianten `small` för att kombinera med andra komponenter t.ex. `Button`, notera hur attributet `isOnColor` används för den primära knappen.

```tsx
<Button>
  <Spinner
    // highlight-start
    small
    isOnColor
    // highlight-end
  />
  Laddar
</Button>
```

<div
  className='card'
  style={{ display: 'block' }}
>
  <Button>
    <Spinner
      small
      isOnColor
    />
    {'Laddar'}
  </Button>
</div>

## Riktlinjer

Om det väntas ta mer än 10 sekunder att ladda sidan bör det förutom en spinner finnas ett meddelande till användaren om det.

För val av laddningsindikator se [Mönster för laddningsindikatorer](/design-patterns/page-loading)

## API

<PropTable name='Spinner' />
