---
title: DropZone
description: Ett generellt mål för drag & drop — inte begränsat till filuppladdning.
---

import { DropZone, FileTrigger, Button, Text, FieldError, Label } from '@midas-ds/components'
import { Upload } from 'lucide-react'
import { ComponentHeader } from '@site/src/components/getComponentMetaData'
import { PropTable } from '@site/src/components/PropsTable'
import { ValidatedDropZoneExample } from '@site/src/components/examples/file-upload/FileUploadExamples'

<ComponentHeader
  name='DropZone'
  friendlyName='Dra och släpp'
/>

En yta som ett eller flera objekt kan dras och släppas på. Används för [filuppladdning](/components/file-upload/file-uploading/#med-drag--drop)

:::warning
Ange alltid `aria-label` på `DropZone`. Annars faller den tillbaka på en oöversatt engelsk standardetikett
("DropZone") som skärmläsare läser upp tillsammans med din egen etikett — oavsett om den kommer från
`slot='label'` eller `aria-labelledby`. De kombineras, de ersätter inte varandra.
:::

```tsx
<DropZone
  onDrop={handleDrop}
  aria-label='Släpp filer här'
>
  <Text slot='label'>Släpp filer här</Text>
</DropZone>
```

<div className='card'>
  <DropZone
    style={{ width: '100%', maxWidth: 480 }}
    aria-label='Släpp filer här'
  >
    <Text slot='label'>Släpp filer här</Text>
  </DropZone>
</div>

## Varianter

### Etikett

Textetiketten inuti ytan beskriver vad som ska släppas eller väljas. Används DropZone som ett fält bland andra i ett
formulär, komplettera med en etikett ovanför.

```tsx
<Label id='upload-label'>Ladda upp filer</Label>
<DropZone
  aria-labelledby='upload-label'
  aria-label='Släpp filer här'
>
  <Text slot='label'>Släpp filer här</Text>
</DropZone>
```

<div className='card'>
  <div style={{ display: 'flex', flexDirection: 'column', width: '100%', maxWidth: 480 }}>
    <Label id='upload-label-docs'>Ladda upp filer</Label>
    <DropZone
      aria-labelledby='upload-label-docs'
      aria-label='Släpp filer här'
    >
      <Text slot='label'>Släpp filer här</Text>
    </DropZone>
  </div>
</div>

### Validering

DropZone kan markeras som ogiltig med `isInvalid`, tillsammans med ett felmeddelande.

```tsx
<DropZone
  isInvalid
  aria-label='Släpp filer här'
>
  <Text slot='label'>Släpp filer här</Text>
</DropZone>
<FieldError isInvalid>Filen är för stor</FieldError>
```

<div className='card'>
  <div style={{ display: 'flex', flexDirection: 'column', width: '100%', maxWidth: 480 }}>
    <DropZone
      isInvalid
      aria-label='Släpp filer här'
    >
      <Text slot='label'>Släpp filer här</Text>
    </DropZone>
    <FieldError isInvalid>Filen är för stor</FieldError>
  </div>
</div>

Samma tillstånd kan användas för att begränsa vilka filtyper som är tillåtna:

<div className='card'>
  <ValidatedDropZoneExample />
</div>

:::tip Typad drop-data
`onDrop` ger en typad `DropEvent` där varje objekt i `items` är en `FileDropItem`, `DirectoryDropItem` eller
`TextDropItem`. Använd type guarden `isFileDropItem` från `react-aria` för att filtrera ut filer, se
[Filuppladdning](/components/file-upload/file-uploading/#med-drag--drop) för ett komplett exempel.
:::

### Inaktiverad

```tsx
<DropZone
  isDisabled
  aria-label='Släpp filer här'
>
  <Text slot='label'>Släpp filer här</Text>
  <FileTrigger>
    <Button
      variant='secondary'
      isDisabled
    >
      <Upload
        aria-hidden
        size={20}
      />
      Välj filer
    </Button>
  </FileTrigger>
</DropZone>
```

<div className='card'>
  <DropZone
    isDisabled
    style={{ width: '100%', maxWidth: 480 }}
    aria-label='Släpp filer här'
  >
    <Text slot='label'>Släpp filer här</Text>
    <FileTrigger>
      {/*
        Demo-only: `secondary` is transparent/outline even when enabled, but its
        disabled state currently reuses the shared washed-fill background meant
        for solid-filled variants (primary/danger). Real fix belongs in
        Button.module.css, tracked separately — this override just keeps the
        docs demo honest to Figma in the meantime.
      */}
      <Button
        variant='secondary'
        isDisabled
        style={{ backgroundColor: 'transparent' }}
      >
        <Upload
          aria-hidden
          size={20}
        />
        Välj filer
      </Button>
    </FileTrigger>
  </DropZone>
</div>

## API

### DropZone

<PropTable name='DropZone' />
