---
id: header-sidebar-panel
title: Header, Sidebar och Panel
sidebar_position: 3
---

# Header, Sidebar och Panel

Kombinationen [Header](./components/header), [Sidebar](./components/sidebar) och [Panel](./components/panel) innehåller alla komponenter för desktopapplikationer. Det är det vanligaste för våra mer avancerade applikationer, där Panel används för att visa kontextuell information t.ex. notifieringar eller mer information om element i en lista.

<style>{`.snabbstart .theme-code-block { max-height: none; }`}</style>

<div className="snabbstart">

```tsx
import {
  Layout,
  LayoutContent,
  Main,
  Header,
  HeaderActions,
  HeaderAction,
  Sidebar,
  Navigation,
  NavigationItem,
  NavigationLink,
  PanelProvider,
  PanelRegion,
} from '@midas-ds/layout'
import { House, Settings, Bell, User } from 'lucide-react'

export default function App() {
  return (
    <PanelProvider panelBehavior='replace'>
      <Layout>
        <Header>
          <HeaderActions>
            <HeaderAction icon={<Bell />}>Notifieringar</HeaderAction>
            <HeaderAction icon={<User />}>Min profil</HeaderAction>
          </HeaderActions>
        </Header>

        <LayoutContent>
          <Sidebar title='Navigation'>
            <Navigation>
              <NavigationItem>
                <NavigationLink
                  href='/'
                  icon={<House />}
                  isActive
                >
                  Hem
                </NavigationLink>
              </NavigationItem>
              <NavigationItem>
                <NavigationLink
                  href='/installningar'
                  icon={<Settings />}
                >
                  Inställningar
                </NavigationLink>
              </NavigationItem>
            </Navigation>
          </Sidebar>

          <Main>{/* Ditt innehåll */}</Main>
          <PanelRegion />
        </LayoutContent>
      </Layout>
    </PanelProvider>
  )
}
```

</div>
