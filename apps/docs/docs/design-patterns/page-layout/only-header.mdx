---
id: header
title: Applikationer utan navigation
sidebar_position: 1
---

import { OnlyHeaderExample } from '@site/src/components/examples/layout/PageLayoutExamples'

# Applikationer utan navigation

För applikationer som inte har någon navigation, tex ensidiga formulär, så används endast vår [Header](/dev/layout/components/header). Headern kan innehålla globala handlingar som t.ex. notiser och logga ut.

<OnlyHeaderExample />

## Exempelkod

```tsx
import { Bell, Settings, User } from 'lucide-react'
import { Header, HeaderAction, HeaderActions, HeaderLogo, HeaderTitle } from '@midas-ds/layout'

export default function App() {
  return (
    <Header>
      <HeaderLogo />
      <HeaderTitle>Mitt system</HeaderTitle>
      <HeaderActions>
        <HeaderAction icon={<Bell size={20} />}>Notiser</HeaderAction>
        <HeaderAction icon={<User size={20} />}>Min profil</HeaderAction>
        <HeaderAction icon={<Settings size={20} />}>Inställningar</HeaderAction>
      </HeaderActions>
    </Header>
    {/* Resten av applikationen */}
  )
}
```
