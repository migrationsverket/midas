{
  "name": "storybook",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/storybook/src",
  "projectType": "application",
  "tags": ["apps"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx storybook dev",
        "cwd": "apps/storybook",
        "port": 4400
      }
    },
    "build": {
      "executor": "@nx/storybook:build",
      "outputs": ["{options.outputDir}"],
      "options": {
        "outputDir": "dist/storybook/ui",
        "configDir": "apps/storybook/.storybook"
      },
      "configurations": {
        "ci": {
          "quiet": true,
          "outputDir": "dist/docs/storybook",
          "webpackStatsJson": "dist/docs/storybook"
        }
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "apps/storybook/vitest.config.ts",
        "project": "interactions"
      },
      "configurations": {
        "watch": {
          "watch": true
        }
      }
    },
    "a11y": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "apps/storybook/vitest.config.ts",
        "project": "a11y:light"
      },
      "configurations": {
        "dark": {
          "config": "apps/storybook/vitest.config.dark.ts",
          "project": "a11y:dark"
        },
        "forced-colors": {
          "config": "apps/storybook/vitest.config.forced-colors.ts",
          "project": "a11y:forced-colors"
        }
      }
    },
    "snapshot": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "apps/storybook/vitest.config.ts",
        "project": "snapshot:light"
      },
      "configurations": {
        "dark": {
          "config": "apps/storybook/vitest.config.dark.ts",
          "project": "snapshot:dark"
        },
        "forced-colors": {
          "config": "apps/storybook/vitest.config.forced-colors.ts",
          "project": "snapshot:forced-colors"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/storybook/**/*.{ts,tsx,js,jsx}"]
      }
    }
  }
}
